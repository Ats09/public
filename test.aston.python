Составить алгоритм: если введенное число больше 7, то вывести “Привет”


number = int(input("Введите число: "))
if number > 7:
    print("Привет")
else:
    print("Число меньше 7")


Составить алгоритм: если введенное имя совпадает с Вячеслав, то вывести “Привет, Вячеслав”, если нет, то вывести "Нет такого имени"

name = input("Введите имя: ")
name = name.lower()

if name == "вячеслав":
    print("Привет, Вячеслав")
else:
    print("Нет такого имени")
    
    
Составить алгоритм: на входе есть числовой массив, необходимо вывести элементы массива кратные 3

numbers_str = input("Введите числа через пробел: ")
numbers = [int(number) for number in numbers_str.split()]

for number in numbers:
    if number % 3 == 0:
        print(f"Число {number} кратное 3")
    else:
        print(f"Число {number} не кратное 3")
        
        
Дана скобочная последовательность: [((())()(())]]
- Можно ли считать эту последовательность правильной?
- Если ответ на предыдущий вопрос “нет” - то что необходимо в ней изменить, чтоб она стала правильной?

Данная скобочная последовательность не является правильной, так как в ней присутствуют ошибки. При сбалансированных скобочных последовательностях каждой открывающей скобке должна соответствовать закрывающая скобка того же типа, и порядок скобок должен быть корректным.

В данной последовательности имеется лишняя закрывающая квадратная скобка в конце. Таким образом, чтобы сделать данную последовательность правильной, необходимо удалить эту лишнюю скобку:

[((())()(())]]

→ [((())()(()))]

Теперь скобочная последовательность является правильной, так как каждой открывающей скобке соответствует закрывающая скобка того же типа и порядок скобок корректен.
